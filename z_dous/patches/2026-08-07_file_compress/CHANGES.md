# 修改日期：2026-08-07

## 修改文件
- `core/ai_client.py` — 压缩时识别文件块并大幅压缩 --- modify_0

## 修改原因
文件内容占据大段上下文，压缩时若均匀处理，摘要仍会保留过多文件细节。

## 修改内容
- `summarize_history()` 检测 `[文件: ...]` 标记
- 若含文件块，在摘要提示词中附加指令：
  每个文件块只需一句话概括主题，不保留具体细节
- 前端已有 `[文件: 名]...[/文件]` 标记（无需改）

## 影响范围
- `core/ai_client.py`：摘要压缩逻辑
